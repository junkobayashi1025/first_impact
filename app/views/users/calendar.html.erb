<div class="container">
  <%= month_calendar events: @reports do |date, reports| %>
    <%= date.day %>
    <% reports.each do |report| %>
      <div>
        <%= report.title %>
      </div>
    <% end %>
  <% end %>
</div>
