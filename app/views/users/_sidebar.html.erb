<h1>～報告書情報～</h1>
<h2>最新ニュース(※全チーム)</h2>
<div class="row">
  <div class="col-md-6">
    <h3>【新規作成】</h3>
    <ul>
      <% @user.new_create_report.each do |reports| %>
      <li><%= link_to "#{reports.title}", report_path(reports.id) %></li>
      <% end %>
    </ul>
  </div>
  <div class="col-md-6">
    <h3>【完了】</h3>
    <ul>
      <% @user.done_report.each do |reports| %>
      <li><%= link_to "#{reports.title}", report_path(reports.id) %></li>
      <% end %>
    </ul>
  </div>
</div>

<h2>所属チーム情報</h2>
<div class="row">
  <div class="col-md-6">
    <h3>【新規作成】</h3>
    <ul>
      <% @user.new_create_report_team.each do |reports| %>
      <li><%= link_to "#{reports.title}", report_path(reports.id) %></li>
      <% end %>
    </ul>
  </div>
  <div class="col-md-6">
    <h3>【完了】</h3>
    <ul>
      <% @user.done_report_team.each do |reports| %>
      <li><%= link_to "#{reports.title}", report_path(reports.id) %></li>
      <% end %>
    </ul>
  </div>
</div>
